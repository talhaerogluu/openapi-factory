apis:
  user1-api:
    root: ./docs/openapi.json

extends:
  - minimal

theme:
  openapi:
    layout: stacked
    theme:
      colors:
        primary:
          main: '#1A73E8'  # user1'e özel renk (istersen değiştiririz)
      logo:
        gutter: 20px

pages:
  - path: /
    api: user1-api